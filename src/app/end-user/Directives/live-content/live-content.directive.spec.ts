import { LiveContentDirective } from './live-content.directive';

describe('LiveContentDirective', () => {
  it('should create an instance', () => {
    const directive = new LiveContentDirective();
    expect(directive).toBeTruthy();
  });
});
