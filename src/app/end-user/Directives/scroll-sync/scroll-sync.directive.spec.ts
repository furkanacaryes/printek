import { ScrollSyncDirective } from './scroll-sync.directive';

describe('ScrollSyncDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollSyncDirective();
    expect(directive).toBeTruthy();
  });
});
